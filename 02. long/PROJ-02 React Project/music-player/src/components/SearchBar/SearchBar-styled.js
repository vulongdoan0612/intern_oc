import styled from "styled-components"

export const Search = styled.div`
    #outlined-basic {
        background: #fff;
        width: 364px;
        height: 40px;
        padding: 0 8px;
        border-radius: 5px;
        overflow: hidden;
    }
    .search fieldset {
        padding: 0;
        border-radius: unset;
    }
    .search .MuiFormLabel-root {
        top: -6px;
    }
`